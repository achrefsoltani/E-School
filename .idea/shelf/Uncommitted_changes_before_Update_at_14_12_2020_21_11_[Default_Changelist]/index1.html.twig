{% extends 'base.html.twig' %}

{% block title %}Absence index{% endblock %}

{% block body %}
    <h1>liste des enseignants</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Numéro du seance</th>
            <th>Nom</th>
            <th>Prenom</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for seance in seances %}
            <tr>
                <td>{{ seance.id }}</td>
                <td>{{ seance.enseignant.nom }}</td>
                <td>{{ seance.enseignant.prenom }}</td>
             <td>
                 <a href="{{ path('absence_new', {'idP':seance.enseignant.id ,'idS': seance.id}) }}">Marquer absence</a>
             </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('search') }}">chercher</a>

{% endblock %}