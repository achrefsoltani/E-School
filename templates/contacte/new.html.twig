{% extends 'form.html.twig' %}

{% block title %}Nouvelle demande{% endblock %}

{% block body %}
    <h1>Créer une nouvelle demande</h1>

    {{ include('contacte/_form.html.twig') }}

    <a href="{{ path('contacte_index') }}"> <button class="btn btn-secondary">{{ button_label|default('Retour a la liste') }}</button></a>
    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>

{% endblock %}
