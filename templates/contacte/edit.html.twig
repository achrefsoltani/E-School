{% extends 'form.html.twig' %}

{% block title %}Modifier contact{% endblock %}

{% block body %}
    <span class="contact100-form-title">
				<h1 class="text-secondary">	Répondre au parent</h1>
				</span>

    {{ include('contacte/_form.html.twig', {'button_label': 'Update'}) }}


    <a href="{{ path('contacte_index') }}"> <button class="btn btn-secondary">{{ button_label|default('Retour a la liste') }}</button></a>
    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>

    {{ include('contacte/_delete_form.html.twig') }}
{% endblock %}
