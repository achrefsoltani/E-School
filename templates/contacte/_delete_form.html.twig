<form method="post" action="{{ path('contacte_delete', {'id': contacte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contacte.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
