{% extends 'base.html.twig' %}

{% block title %}Edit News{% endblock %}

{% block body %}
    <span class="contact100-form-title">
				<h1 class="text-secondary">	Modifier l'accueil </h1>
				</span>

    {{ include('news/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('news_index') }}">back to list</a>
    <a href="{{ path('news_index') }}"> <button class="btn btn-secondary">{{ button_label|default('Retour a la liste') }}</button></a>
    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>

    {{ include('news/_delete_form.html.twig') }}
{% endblock %}
