{% extends 'form.html.twig' %}

{% block title %}Edit {{ role}} {% endblock %}

{% block body %}
    <span class="contact100-form-title">
				<h1 class="text-secondary">	Modifier un personne</h1>
				</span>

    {{ include('personne/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="/list/{{ role }}"> <button class="btn btn-secondary">{{ button_label|default('Retour a la liste') }}</button></a>
    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>



    {{ include('personne/_delete_form.html.twig') }}
{% endblock %}
