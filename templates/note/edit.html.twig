{% extends 'form.html.twig' %}

{% block title %}Edit Note{% endblock %}

{% block body %}
    <span class="contact100-form-title">
				<h1 class="text-secondary">	Modifier une note</h1>
				</span>

    {{ include('note/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>

{#    <a href="{{ path('note_index') }}">back to list</a>#}

    {{ include('note/_delete_form.html.twig') }}
{% endblock %}
