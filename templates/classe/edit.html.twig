{% extends 'form.html.twig' %}

{% block title %}Modification Classe{% endblock %}

{% block body %}
    <span class="contact100-form-title">
				<h1 class="text-secondary">	Modifier un classe</h1>
				</span>

    {{ include('classe/_form.html.twig', {'button_label': 'Update'}) }}



    <a href="{{ path('classe_index') }}"> <button class="btn btn-secondary">{{ button_label|default('Retour a la liste') }}</button></a>
    <a href="{{ path('news_home') }}">  <button type="button" class="btn btn-secondary" >Accueil</button> </a>
    {{ include('classe/_delete_form.html.twig') }}
{% endblock %}
