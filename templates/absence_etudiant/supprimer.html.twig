{% extends 'base.html.twig' %}

{% block title %}supprimer l'absence{% endblock %}

{% block body %}
    <h1>annuler l'absence</h1>
    {% for message in app.session.flashBag.get('error') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}
    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ absence.id|default('') }}</td>
        </tr>
        <tr>
            <th>Nom </th>
            <td>{{ absence.personne.nom|default('') }}</td>
        </tr>
        <tr>
            <th>Prénom</th>
            <td>{{ absence.personne.prenom |default('')}}</td>
        </tr>
        </tbody>
    </table>

    {{ include('absence_etudiant/_delete_form.html.twig', {'button_label': 'supprimer'}) }}
    <div>
        <br>
    <a href="{{ path('absence_etudiant_index',{'id':absence.seance.id}) }}"><button class="btn btn-info">retour</button> </a>
    </div>
{% endblock %}